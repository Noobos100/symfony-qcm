{% extends 'base.html.twig' %}

{% block title %}Hello {{ email_first_part }}!{% endblock %}

{% block body %}
    <style>
        .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
        .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
        table { width: 100%; border-collapse: collapse; }
        table, th, td { border: 1px solid black; }
        th, td { padding: 8px; text-align: left; }
    </style>

    <div class="example-wrapper">
        <h1>Hello {{ email_first_part }}! ✅</h1>

        This friendly message is coming from:
        <ul>
            <li>Your controller at <code>/home/cdaw/qcm/src/Controller/QcmController.php</code></li>
            <li>Your template at <code>/home/cdaw/qcm/templates/qcm/index.html.twig</code></li>
        </ul>

        <!-- Table containing users from the API -->
        <table>
            <thead>
            <tr>
                <th>id</th>
                <th>email</th>
            </tr>
            </thead>
            <tbody>
            {% for user in users %}
                <tr>
                    <td>{{ user.id }}</td>
                    <td>{{ user.email }}</td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    </div>
{% endblock %}